[project]
name = "project1"
version = "0.1.0"
description = "First fully personalized agentic project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.2.0",
    "langchain-community>=0.4.1",
    "langmem>=0.0.30",
]
